language: node_js
node_js:
  - "8.9.3"
before_install:
  - npm install -g eslint istanbul coveralls
script:
  - npm test --verbose
after_success:
  - cat ./coverage/lcov.info | coveralls && rm -rf ./coverage
